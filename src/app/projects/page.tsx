import ProjectHub from "@/components/pages/project/project-hub";

export const metadata = {
    title: "Projects",
};

export default function ProjectsPage() {
    return (
        <div>
            <ProjectHub />
        </div>
    );
}